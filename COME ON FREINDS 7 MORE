#include<stdio.h>
int calclength(char[]);
int main()
{
	char str[30];
	printf("Enter the string=");
    gets(str);
	int l=calclength(str);
	printf("length=%d",l);
	return 0;
}
int calclength(char s[])
{
	int i,count=0;
	for(i=0;s[i]!='\0';i++)
	count++;
	return count;
}
