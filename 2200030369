#include<stdio.h>
main()
{
	int n,sum=0,r,temp;
	printf("enter n\n");
	scanf("%d",&n);
	temp=n;
	while(n!=0)
	{
		r=n%10;
		sum=(r*r*r)+sum;
		n=n/10;
	}
	if(sum==temp)
	{
		printf("armstrong num");
	}
	else
	{
		printf(" not an armstrong num");
	}
}
